import React, { Component } from "react";

const NotificationComponent = ({ className, children }) => {
  return <div className={"notification " + className}>{children}</div>;
};

export default NotificationComponent;

import React from "react";

import { storiesOf, configure, addDecorator } from "@storybook/react";
import { action } from "@storybook/addon-actions";
import { linkTo } from "@storybook/addon-links";

import "bulma/css/bulma.css";

addDecorator(story => (
  <section className="section">
    <div className="container">{story()}</div>
  </section>
));

// configure(function() {
//   // ...
// }, module);

import {
  FileInput,
  Input,
  LoadingComponent,
  NotificationComponent,
  Select,
  Textarea
} from "../";

storiesOf("FileInput", module).add("FileInput", () => (
  <FileInput
    name="name"
    label="label"
    fileName="fileName"
    fileLabel="fileLabel"
  />
));

storiesOf("Input", module).add("Input", () => (
  <div>
    <Input name="name" label="label" placeholder="placeholder" />
    <Input
      name="name"
      label="label"
      className="is-danger"
      help={true}
      helpValue="helpValue"
    />
  </div>
));

storiesOf("LoadingComponent", module).add("LoadingComponent", () => (
  <LoadingComponent />
));

storiesOf("NotificationComponent", module).add("NotificationComponent", () => (
  <div>
    <NotificationComponent>children</NotificationComponent>
    <NotificationComponent className="is-warning">
      className="is-warning"
    </NotificationComponent>
    <NotificationComponent className="is-info">
      className="is-info"
    </NotificationComponent>
    <NotificationComponent className="is-success">
      className="is-success"
    </NotificationComponent>
  </div>
));

storiesOf("Select", module).add("Select", () => <Select />);

storiesOf("Textarea", module).add("Textarea", () => (
  <Textarea name="name" label="label" help={true} helpValue="helpValue" />
));
